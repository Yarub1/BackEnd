// File: sync.js
/*
const { sequelize } = require("../Models");

sequelize
  .sync({ force: true }) // Use { force: true } with caution as it will recreate the tables and result in data loss.
  .then(() => {
    console.log("All models were synchronized successfully.");
  })
  .catch((err) => {
    console.error("Failed to synchronize models:", err);
  });
*/